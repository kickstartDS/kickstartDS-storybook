import { Footer } from "./FooterComponent";

const Template = (args) => <Footer {...args} />;

export default {
  title: "Base/Footer",
  component: Footer,
};

export const Default = Template.bind({});
