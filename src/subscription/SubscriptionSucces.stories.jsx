import { SubscriptionSucces } from "./SubscriptionSuccesComponent";

const Template = (args) => <SubscriptionSucces {...args} />;

export default {
  title: "Form/Subscription",
  component: SubscriptionSucces,
};

export const Succes = Template.bind({});
