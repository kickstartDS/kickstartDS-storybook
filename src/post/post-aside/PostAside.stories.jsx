import { PostAside } from "./PostAsideComponent";

const Template = (args) => <PostAside {...args} />;

export default {
  title: "Blog/Post Aside",
  component: PostAside,
};

export const Default = Template.bind({});
