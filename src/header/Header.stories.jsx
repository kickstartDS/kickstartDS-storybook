import { Header } from "./HeaderComponent";

const Template = (args) => <Header {...args} />;

export default {
  title: "Base/Header",
  component: Header,
};

export const Default = Template.bind({});
